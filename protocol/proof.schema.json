{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["jobId", "nodeId", "rangeStart", "rangeEnd", "counter", "operations", "resultDigest"],
  "properties": {
    "jobId": {"type": "string", "minLength": 1},
    "nodeId": {"type": "string", "minLength": 1, "maxLength": 128},
    "rangeStart": {"type": "integer", "minimum": 0},
    "rangeEnd": {"type": "integer", "minimum": 0},
    "counter": {"type": "integer", "minimum": 0},
    "operations": {"type": "integer", "minimum": 1},
    "resultDigest": {"type": "string", "minLength": 64, "maxLength": 64},
    "matchAddress": {"type": ["string", "null"], "maxLength": 42}
  },
  "additionalProperties": false
}
